import { useEffect } from "react";
import { Box, Button, Drawer, DrawerBody, DrawerCloseButton, DrawerContent, DrawerFooter, DrawerHeader, Flex, Text } from "@chakra-ui/react";
import NoLocations from "./noLocations";
import useLocalStorage from "@/hooks/useLocalStorage";

type Props = {
  w?: string;
  borderLeft?: string;
  display?: string;
};

const Index = (props: Props) => {
  return (
    <>
      
    </>
  );
};

export default Index;
